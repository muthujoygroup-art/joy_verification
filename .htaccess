<IfModule mod_rewrite.c>
  RewriteEngine On
  
  # If requested path is not in dist, rewrite internally to dist/
  RewriteCond %{REQUEST_URI} !^/dist/
  RewriteCond %{DOCUMENT_ROOT}/dist%{REQUEST_URI} -f [OR]
  RewriteCond %{DOCUMENT_ROOT}/dist%{REQUEST_URI} -d
  RewriteRule ^(.*)$ /dist/$1 [L]

  # Route all other root requests to dist/index.html for React SPA
  RewriteCond %{REQUEST_URI} !^/dist/
  RewriteRule ^(.*)$ /dist/index.html [L]
</IfModule>
